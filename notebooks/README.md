# Notebooks

Thư mục này dùng cho notebook demo tùy chọn.

Production code vẫn nên nằm trong `src/`. Không nên viết toàn bộ logic của agent hoặc workflow trong notebook, vì như vậy sẽ khó test, khó trace và khó tái sử dụng.
